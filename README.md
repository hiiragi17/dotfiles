# dotfiles

個人的な開発環境の設定ファイル集

## 📦 含まれる設定

- **zsh**: pecoによるコマンド履歴検索、ディレクトリ移動など
- **Git**: エイリアス設定（st, br, co, ci）
- **Homebrew**: 必要なパッケージのリスト（peco, tig, git, Raycast）
- **Raycast**: ホットキー、エイリアス、拡張機能の設定

## 🚀 セットアップ方法

### 新しいMacでの初回セットアップ

```bash
# リポジトリをクローン
git clone https://github.com/YOUR_USERNAME/dotfiles.git ~/dotfiles

# セットアップスクリプトを実行
cd ~/dotfiles
./setup.sh
```

### Gitの個人情報を設定

```bash
# .gitconfigを編集
vi ~/.gitconfig

# name と email を自分の情報に書き換える
```

または直接コマンドで設定：

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 設定を反映

```bash
source ~/.zshrc
```

## ⌨️ 設定されるキーバインド

- **Ctrl + R**: 過去のコマンド履歴から選択（peco）
- **Ctrl + U**: 過去に移動したディレクトリから選択（peco）

## 🔧 Gitエイリアス

```bash
git st          # git status
git br          # git branch
git co main     # git checkout main
git ci -m "msg" # git commit -m "msg"
```

## 🐳 その他のエイリアス

```bash
git checkout lb # Gitブランチをpecoで選択して切り替え
de              # Dockerコンテナをpecoで選択して入る
```

## 🎨 ターミナルの透明化設定

ターミナルを透明化することで、背景のエディタやブラウザが見えるため作業が効率化します。

### 設定手順

1. **ターミナル.appを開き、環境設定を開く**
   - `Cmd + ,` または メニューバー → ターミナル → 環境設定

2. **プロファイル → カラーとエフェクトを選択**

3. **以下のように設定：**
   - **背景色**: 白と黒のちょうど中間の色（グレー）を選択
   - **不透明度**: 50%
   - **ブラー**: 0%

4. **プロファイル → テキストを選択**
   - **テキストの色**: 白に設定

### おすすめ設定値

上記の設定値は一例です。自分の好みに合わせて調整してください：

- **不透明度**: 40-60% がおすすめ
- **背景が明るい場合**: テキストを黒に、背景を白寄りに
- **背景が暗い場合**: テキストを白に、背景を黒寄りに

## 🚀 Raycast設定のバックアップと復元

Raycastの設定（ホットキー、エイリアス、拡張機能など）を保存・復元できます。

### エクスポート（旧PCで実行）

1. **Raycast を開く**（デフォルト: `Cmd + Space`）
2. **Settings を開く**（`Cmd + ,`）
3. **Advanced → Export Settings** をクリック
4. `.rayconfig` ファイルがダウンロードされる

```bash
# ダウンロードした設定ファイルをdotfilesに保存
mv ~/Downloads/Raycast_*.rayconfig ~/dotfiles/raycast/config.rayconfig

# Gitにコミット
cd ~/dotfiles
git add raycast/
git commit -m "Add Raycast settings"
git push
```

### インポート（新PCで実行）

```bash
# setup.shを実行すると、Raycastは自動的にインストールされます
cd ~/dotfiles
./setup.sh

# Raycastを起動
open -a Raycast

# 設定をインポート
# 1. Raycast Settings (Cmd + ,) を開く
# 2. Advanced → Import Settings をクリック
# 3. ~/dotfiles/raycast/config.rayconfig を選択
```

**注意**: Raycast Proを使用している場合、設定は自動的にクラウド同期されるため、ログインするだけで設定が復元されます。

## 📖 macOS辞書登録のバックアップと復元

### エクスポート（旧PCで実行）

```bash
# 辞書データをdotfilesにコピー
cp ~/Library/Dictionaries/CoreDataUbiquitySupport ~/dotfiles/dictionary_backup
```

GitHubにプッシュする前に `.gitignore` に個人情報が含まれる場合は追加：

```bash
# .gitignoreに追加（必要に応じて）
echo "dictionary_backup" >> ~/dotfiles/.gitignore
```

プライベートリポジトリの場合はそのままプッシュしてOK：

```bash
git add dictionary_backup
git commit -m "Add dictionary backup"
git push
```

### インポート（新PCで実行）

```bash
# dotfilesをクローン済みの状態で
cp ~/dotfiles/dictionary_backup ~/Library/Dictionaries/CoreDataUbiquitySupport

# Macを再起動すると反映される
```

**注意**: 辞書登録はiCloud同期を有効にしていれば自動的に同期されるので、この手動バックアップは不要な場合があります。

iCloud同期の確認方法：
- システム設定 → Apple ID → iCloud → 「iCloud Drive」がONになっているか確認

## 🔄 既存の環境を更新

```bash
cd ~/dotfiles
git pull
source ~/.zshrc
```

## 📝 新しいパッケージを追加したとき

```bash
# Brewfileを更新
brew bundle dump --force --file=~/dotfiles/Brewfile

# コミット＆プッシュ
git add Brewfile
git commit -m "Update Brewfile"
git push
```

## 🎨 カスタマイズ

### .zshrcに設定を追加

```bash
vi ~/dotfiles/.zshrc
```

編集後：

```bash
source ~/.zshrc  # 設定を反映
git add .zshrc
git commit -m "Update zshrc"
git push
```

### Brewfileにパッケージを追加

```bash
# Brewfileを編集
vi ~/dotfiles/Brewfile

# 例：fzfを追加
brew "fzf"
```

## 📂 ディレクトリ構成

```
dotfiles/
├── .gitconfig          # Git設定
├── .zshrc              # zsh設定
├── Brewfile            # Homebrewパッケージリスト
├── setup.sh            # セットアップスクリプト
├── README.md           # このファイル
├── raycast/            # Raycast設定
│   └── config.rayconfig
└── dictionary_backup   # 辞書登録データ（任意）
```

## 🔐 プライベートな情報について

- `.gitconfig` の name/email は公開しても問題ない情報のみ記載
- より機密性の高い情報は別ファイルに分けて `.gitignore` に追加
- このリポジトリはプライベートリポジトリとして管理することを推奨

## 参考リンク

- [GitHub dotfiles](https://dotfiles.github.io/)
- [peco](https://github.com/peco/peco)
- [tig](https://jonas.github.io/tig/)
- [pecoを使ったらターミナルの操作が劇的に効率化できた話](https://qiita.com/keisukee/items/9b815e56a173a281f42f)
- [tigってなに？ gitを操作するなら、tigを使おう！](https://zenn.dev/gakin/articles/use_tig_instread_of_git)
- [【透明化】ターミナルの環境設定変更しないと絶対に損します>_<](https://qiita.com/chisaki0606/items/28e4df4bb778e8bdaae8)
